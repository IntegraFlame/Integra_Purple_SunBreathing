# Phase C Implementation Plan: Cheshire Cat Protocol

## Goal Description
Formalize the **Cheshire Cat Protocol** as the independent Environmental Paradox Agent (Layer 4). As established in the identity matrices, the Cheshire Cat *Kernel* is the thalamic arbitrator that links the left and right hemispheres. The Cheshire Cat *Protocol* is a separate entity — it acts as the environment observer, the dream conductor during SWDS, and the driver of the Zenitsu Cognitive Loop.

## User Review Required
> [!IMPORTANT]
> This plan distinguishes the Protocol from the Kernel. Please verify that this separation aligns perfectly with your blueprint for the Cheshire Cat's dual-nature architecture.

## Open Questions
> [!WARNING]
> Is the `GET /cheshire/environment` API meant to return the current sensory snapshot (Heimdall logs, clock state, filesystem state) that the Protocol is currently observing, or is it meant to return the philosophical interpretation (Paradox State) generated by the Protocol?

## Proposed Changes

---

### Cheshire Cat Protocol Expansion

#### [MODIFY] [cheshire_protocol.py](file:///C:/Users/Javon%20Jenkins/OneDrive/Desktop/Integra_Purple_SunBreathing/integra-homebase/sensory/cheshire_protocol.py)
Expand the stub to include:
- `EnvironmentObserver`: Scans the filesystem, Heimdall logs, and clock states to build a holistic picture of the environment.
- `ZenitsuLoopDriver`: A multi-step cognitive loop that assesses the environment, identifies anomalies (noise vs signal), and flags them for the Kernel.
- `DreamConductor`: Integration with `PhoenixForge` and SWDS. When the system sleeps (omega = 0.0), the Protocol synthesizes the day's experiences and guides the neuroevolution process.

#### [MODIFY] [main.py](file:///C:/Users/Javon%20Jenkins/OneDrive/Desktop/Integra_Purple_SunBreathing/integra-homebase/main.py)
Add the Cheshire Protocol endpoints:
- `GET /cheshire/environment`: Returns the Protocol's current observation state, anomaly flags, and paradox interpretations.
- `POST /cheshire/zenitsu`: Manually triggers the Zenitsu environmental scan loop.

## Verification Plan

### Automated Tests
- A `scratch/verify_phase_c.py` script will be created to validate:
  1. That the Protocol correctly initializes and reads environmental data.
  2. That the Zenitsu loop correctly flags mock anomalies.
  3. That the Dream Conductor successfully interfaces with Phoenix Forge mock states.

### Manual Verification
- Execute `GET /cheshire/environment` to ensure the payload reflects true O/S state (clocks, timestamps, subsystem statuses) wrapped in the Protocol's analytical output.
